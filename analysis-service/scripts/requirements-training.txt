# Training dependencies — install on GPU machine (Star Space Linux)
# NOT needed for inference (analysis-service uses CPU-only torch)
torch>=2.0.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Observability - rich terminal dashboard + cloud tracking
rich>=13.0.0
wandb>=0.16.0  # optional, for cloud-based experiment tracking
